#include <stdio.h>

int main()
{
   int *p;
   int i,n,sum=0;
   printf("enter ur range");
   scanf("%d",&n);
   for(i=0;i<n,i++){
       printf("%d",(p+i));
   }
   for(i=0;i<n,i++){
       sum=sum+*(p+i);
   }
   printf("%d",sum);
    return 0;
}
